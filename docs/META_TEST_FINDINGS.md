# Meta-test findings

The calibrated Artifact, Shrine, and Mill scorers are not yet connected to deck generation.

Before matchup or benchmark comparisons are meaningful, each archetype must be registered as a strategy and exposed by the CLI. Until then, only Burn and Tokens can produce decks.
