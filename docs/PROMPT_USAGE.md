# Prompt Usage

For future calibration runs, use this short external instruction:

> Work in `derSimon1/thun-format-deckbuilder`. Follow the current repository versions of `docs/SPECIFICATION.md` and `docs/PROMPTS/global-calibration.md` as the binding source of truth. Read and update the development logbook and roadmap. Run a global calibration for X hours on the specified branch and pull request.

For the current calibration branch:

> Work in `derSimon1/thun-format-deckbuilder` on branch `codex/global-deckbuilder-calibration` and PR #14. Follow `docs/SPECIFICATION.md` and `docs/PROMPTS/global-calibration.md`. Run a global calibration for X hours. Keep PR #13 untouched.
