# Archive Policy

When documents become large:

- keep current rules in the main files
- archive old logbook months under `docs/LESSONS/`
- preserve old specification versions through Git history and changelog
- never delete a rejected hypothesis without recording why it was rejected
